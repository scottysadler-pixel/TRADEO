# The live Trade1 app (standalone/) uses free, no-key public APIs via Vercel
# serverless functions, so no environment variables are required for production.
#
# This file exists as a placeholder in case you add an optional upstream that
# needs a key in future (e.g. a premium market-data feed). Copy to .env locally
# and never commit real keys.
